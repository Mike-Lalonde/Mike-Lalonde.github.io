export async function askAssistant(question) {
  // Future: replace this with a real backend call, e.g.
  // const res = await fetch("https://your-api/ask", { method:"POST", ... })
  // return (await res.json()).answer;

  return `Assistant is coming soon. You asked: "${question}"`;
}